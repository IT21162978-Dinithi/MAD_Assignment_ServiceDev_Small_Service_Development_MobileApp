<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MAD_MiniProject"
        tools:targetApi="31">
        <activity
            android:name=".activities.signUpActivity"
            android:exported="false" />

        <activity
            android:name=".activities.signInActivity"
            android:exported="false" />

        <activity
            android:name=".activities.frechBookingActivity"
            android:exported="false" />
        <activity
            android:name=".activities.dashboardActivity"
            android:exported="false" />
        <activity
            android:name=".activities.editBookingActivity"
            android:exported="false" />
        <activity
            android:name=".activities.BookingDetailedActivity"
            android:exported="false" />
        <activity
            android:name=".activities.addBookingActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>